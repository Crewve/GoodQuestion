@AGENTS.md

# 개발 기준 문서 우선순위 — SSOT (2026-08-10 확정)

모든 개발 판단은 아래 우선순위를 따른다:

1. **기본**: `docs/`의 설계 문서를 따른다 — 기획 문서(기능명세서·정보구조도·미션 노출 기준·UX 핸드오프 가이드)와 Notion 「DB 구조_260803_수정안」 DB 설계서(원문 덤프: `fixtures/raw/notion_DB구조_260803_수정안.txt`).
2. **문서 간 충돌 시**: `docs/기획/기능명세서/`의 **기능명세서가 최우선 기준**이다. DB 설계서·다른 문서와 어긋나면 기능명세서 요건을 충족하는 방향으로 구현한다.
3. **충돌 기록**: 충돌을 발견하면 `specs/001-goodquestion-mvp/`(research·data-model·tasks)에 즉시 기록하고 팀에 공유한다. 어긋난 문서(Notion 설계서 등)는 사후 개정으로 정합을 맞춘다.
4. **DB 스키마 변경 절차**: 충돌 해소로 스키마 변경이 필요한 경우에도 **적용 전에 팀 확인**을 거친다. 임의 선적용 금지. 적용이 확정되면 Notion 설계서 개정에도 반영한다.

# UI 기준 — SSOT (2026-08-13 확정)

모든 UI 작업(화면 구현·스타일·레이아웃·에셋·폰트·색상)은 `design/` 폴더의 문서와 파일을 기준으로 한다.

1. **수치·색상 등 세부 값의 최우선 기준은 피그마**: `design/GoodQuestion.fig` (구현 기준은 「개발 배포용」 페이지, [웹 링크](https://www.figma.com/design/2QVzNfJldLCvRK7teKCRWX/GoodQuestion_Free?node-id=244-3134)). 크기·간격·색상 값은 임의로 정하지 말고 항상 피그마에서 추출한다.
2. **반응형·인터랙션·레이아웃 원칙**: `design/UX UI 디자인-개발 핸드오프 가이드.md`를 따른다 — 태블릿 가로 1194×834 시안 기준, 우선순위 태블릿→PC→모바일 가로, 아이 화면은 스크롤 미허용, 보호자 화면은 Header/GNB 고정 + 세로 스크롤 허용. (`docs/기획/UX/`에 있는 동명 문서는 같은 내용의 사본이며, 어긋나면 `design/` 버전을 기준으로 한다.)
3. **디자인 토큰·컴포넌트 규칙**: `design/디자인시스템/굿퀘스천_디자인시스템_V1.svg` (파일명은 V1이나 내용은 V2). 컬러 토큰: Base `#FFF8EE` · Primary `#FF7A3D` · Sunny `#FFC93C` · Sage+ `#3DBE8B` · Sky `#4FA9E8` · Berry `#F262A0` · Text `#3A2C1E`.
4. **에셋**: 이미지는 `design/이미지/`의 원본 PNG를 사용하고, 새 일러스트를 임의 생성하지 않는다. 폰트는 `design/폰트/` 원본에서 추출해 커밋된 `src/fonts/`의 webfont(woff2)를 사용한다. 자세한 배치 규칙은 `design/README.md` 참고.
5. **범위 구분**: 기능 요건·동작 충돌은 위 「개발 기준 문서 우선순위」(기능명세서 최우선)를 따르고, 시각·레이아웃 판단은 이 UI SSOT를 따른다.
6. **주의**: `design/`의 대용량 파일은 git 미추적이다(README만 커밋). 파일이 없으면 임의 대체물을 만들지 말고 팀 공유 채널에서 전달받아 배치한다.
