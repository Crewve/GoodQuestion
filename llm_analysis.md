# 중간 LLM/아키텍처 선정 분석 보고서

프로젝트: **굿퀘스천** — 아동 음성대화 학습 서비스 (해커톤 MVP)
작성일: 2026-08-05 · 근거 문서: `llm_context.md`

---

## 1. 전제 요약

- **역할 분해**: ① 발화 분석 LLM(아이 텍스트 → JSON 분석) ② 캐릭터 질문 생성 LLM(부족 요소 유도, GUIDED) ③ **서버 규칙(코드)** — missing 계산·턴 카운트·모드/종료 결정. **판단은 코드, LLM은 분석·생성만**이 자료의 명시적 설계 원칙.
- **사고 요소**: DECISION, REASON, PERSPECTIVE, SOLUTION, RESULT, EMOTION, EMPATHY, REQUEST (감정분석 = EMOTION 요소 추출).
- **비용 산정 시나리오**: 데모 규모 세션 10턴 × 50세션 = **500턴** 가정. 요금은 2026-08 조사 기준 개략치(제출 전 각 벤더 요금표 재확인).
- **주변 결정과의 연계**: STT는 OpenAI Whisper API 채택(`stt_analysis.md`), TTS는 타입캐스트(`tts_analysis.md`) — OpenAI 키·SDK가 이미 프로젝트에 존재.

---

## 2. 역할 통합안 vs 분리안 비교

| 축 | **통합안** — gpt-4o-mini 단일, 프롬프트만 2종 | **분리안** — 분석=소형 / 생성=중급 |
|---|---|---|
| **비용** (1턴) | 분석 ≈ $0.0003 + 생성 ≈ $0.0003 → **≈ $0.0006 (약 0.8원)**. 500턴 ≈ **$0.3** | 분석 ≈ $0.0003 + 생성(gpt-4o) ≈ $0.005 → **≈ $0.0055 (약 8원)**. 500턴 ≈ **$2.7** |
| **품질** | 분석 JSON은 소형으로 충분(structured output 지원). 캐릭터 질문의 톤·연기력이 다소 밋밋할 수 있음 — 단, 서버가 부족 요소를 지정해 프롬프트 제약이 강하므로 격차는 축소됨 | 생성 쪽 페르소나 유지·문장 자연스러움 우위. 아동 안전 톤 관리도 중급 모델이 더 안정적 |
| **개발 난이도** | **최저**: 모델 1개, 키 1개, 오류 처리·재시도 로직 1벌 | 소폭 상승: 호출 경로 2개 — 단 같은 벤더면 모델명 문자열만 다른 동일 클라이언트라 실질 차이는 작음 |
| **지연시간** | 분석·생성 모두 소형이라 턴 파이프라인(분석→규칙→생성→TTS)이 가장 빠름 | 생성 단계 +0.5~1초 수준 |
| **적합 시점** | 해커톤 초기 구축·데모 기본값 | 리허설에서 캐릭터 질문 품질이 부족하다고 판단될 때 |

> **절충(권장)**: 코드에서 역할별 `model` 파라미터를 설정값으로 분리해 두고, **시작은 통합(mini), 품질 미달 시 생성만 상위 모델로 스위치**. 두 안의 차이가 설정 한 줄이 되도록 만드는 것이 핵심.

---

## 3. 역할별 추천 모델과 1턴 비용 추정

토큰 가정 — 분석: 입력 ≈ 1,000토큰(시스템 프롬프트+요소 정의+장면 컨텍스트+아이 발화), 출력 ≈ 200토큰(JSON). 생성: 입력 ≈ 1,500토큰(페르소나+최근 대화+부족 요소), 출력 ≈ 150토큰(질문 1~2문장).

### 발화 분석 (소형·저가·JSON 안정성 우선)

| 모델 | 요금 ($/1M in / out) | 1턴 비용 | 비고 |
|---|---|---|---|
| **gpt-4o-mini** ★추천 | ~0.15 / 0.60 | **≈ $0.0003 (0.4원)** | structured output(strict json_schema) 안정, Whisper와 동일 키·SDK |
| gpt-4.1-mini | ~0.40 / 1.60 | ≈ $0.0007 | mini 대비 지시 이행력 소폭 우위 |
| Claude Haiku 4.5 | 1.00 / 5.00 | ≈ $0.002 (2.8원) | `output_config.format`으로 스키마 강제 가능, 한국어 준수 |
| Gemini Flash | ~0.10 / 0.40 | ≈ $0.0002 | 최저가, responseSchema 지원. 벤더 추가 비용(키·SDK)이 단점 |

### 캐릭터 질문 생성 (톤·창의성, 아동 안전)

| 모델 | 요금 ($/1M in / out) | 1턴 비용 | 비고 |
|---|---|---|---|
| **gpt-4o** ★추천(분리 시) | ~2.50 / 10.00 | **≈ $0.005 (7원)** | 한국어 캐릭터 톤 무난, 분석과 동일 벤더 |
| Claude Sonnet 5 | 2.00 / 10.00 (인트로가, ~2026-08-31; 정가 3/15) | ≈ $0.0045 | 페르소나 유지·안전성 강점. 벤더 추가가 단점 |
| Gemini Pro | ~1.25 / 10.00 | ≈ $0.0034 | 동급 품질, 벤더 추가 단점 동일 |
| gpt-4o-mini (통합 시) | ~0.15 / 0.60 | ≈ $0.0003 | 서버가 부족 요소를 지정하므로 제약이 강해 데모 수준에선 수용 가능 |

**공통 절감 팁**: 시스템 프롬프트(요소 정의·페르소나)는 고정 텍스트이므로 프롬프트 캐싱 대상(OpenAI 자동 캐싱 / Claude `cache_control`). 데모 규모에선 금액 영향이 미미하지만, 고정부를 프롬프트 앞쪽에 두는 구조만 지키면 무료로 얻는 최적화다.

---

## 4. 오케스트레이션 다이어그램 (한 턴) — 서버 규칙 vs LLM 책임 경계

```
[아이 발화 종료 · '보내기' 터치]
          │
          ▼  (STT 파이프라인: 힌팅→실패게이트→후처리, stt_analysis.md)
   STT 확정 텍스트 (messages.text)
          │
          ▼
┌─────────── LLM ① 발화 분석 ───────────┐
│ 입력: 확정 텍스트 + 장면 컨텍스트        │   ★ 분석만 한다.
│      + 직전 캐릭터 질문                 │     "무엇이 부족한가"를
│ 출력: JSON — child_intent, main_point, │     여기서 판단하지 않음
│   detected_elements[], validity        │
└──────────────────┬────────────────────┘
                   ▼
        utterance_analyses 저장
                   ▼
┌─────────── 서버 규칙 (코드, LLM 아님) ───────────────┐
│ 1. accumulated_elements ∪= detected_elements       │
│ 2. missing_elements = required − accumulated       │
│ 3. 턴 카운트 증가                                    │
│ 4. 저정보 연속 판정 (SHORT/UNCLEAR 연속 N회)          │
│ 5. 응답 모드 확정: NORMAL / GUIDED / CLOSING         │
│ 6. 장면 목표 충족 여부·종료 이유 확정                  │
└─────────┬──────────────────────┬───────────────────┘
       CLOSING              NORMAL / GUIDED
          │                      │
          ▼                      ▼
 character_closing      ┌─ LLM ② 캐릭터 질문 생성 ──────┐
 고정 대사 mp3 재생      │ 입력: 캐릭터 페르소나          │
 (사전 생성, 과금 0)     │   + 서버가 지정한 부족요소 1~2개│
                        │   + 최근 대화 2~3턴            │
                        │ 출력: 캐릭터 톤 질문 1~2문장     │  ★ 생성만 한다.
                        └──────────────┬───────────────┘     어떤 요소를 유도할지는
                                       ▼                      서버가 이미 지정
                          서버 후검증(길이·금칙어)
                                       ▼
                          타입캐스트 TTS(런타임) → 재생
```

**책임 경계 원칙** (자료 명시 사항의 구현 규칙화):

- LLM ①은 관찰 결과만 보고한다 — 부족 요소 계산·모드 결정을 프롬프트에 넣지 않는다.
- LLM ②는 서버가 넘겨준 부족 요소(최대 1~2개)만 유도한다 — "이제 끝낼까?"를 스스로 판단하지 않는다.
- 종료·모드·카운트는 전부 코드: 시연 중 재현 가능성과 디버깅 용이성이 보장되고, LLM 호출 실패 시에도 세션 흐름(재시도·폴백)이 코드로 제어된다.

---

## 5. 발화 분석 JSON 출력 스키마와 프롬프트 뼈대

### 출력 스키마 (structured output으로 강제)

OpenAI `response_format: {type: "json_schema", strict: true}` / Claude `output_config.format` 에 그대로 사용 — 파싱 실패 재시도 로직이 불필요해진다.

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["child_intent", "main_point", "detected_elements", "utterance_validity"],
  "properties": {
    "child_intent": {
      "type": "string",
      "description": "아이가 이 발화로 하려던 것 (한 문장)"
    },
    "main_point": {
      "type": "string",
      "description": "발화의 핵심 내용 요약 (한 문장)"
    },
    "detected_elements": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["element", "evidence"],
        "properties": {
          "element": {
            "type": "string",
            "enum": ["DECISION", "REASON", "PERSPECTIVE", "SOLUTION", "RESULT", "EMOTION", "EMPATHY", "REQUEST"]
          },
          "evidence": {
            "type": "string",
            "description": "아이 발화에서 그대로 인용한 근거 구절"
          }
        }
      }
    },
    "utterance_validity": {
      "type": "string",
      "enum": ["VALID", "SHORT", "UNCLEAR", "OFF_TOPIC", "PLAYFUL"]
    }
  }
}
```

### 시스템 프롬프트 뼈대

```
너는 아동 발화 분석기다. 만 5~9세 아이가 동화 캐릭터의 질문에 음성으로 답한
내용(STT 변환·교정본)을 분석해 JSON만 출력한다.

[사고 요소 정의 — 확실히 드러난 것만 감지]
- DECISION: 어떻게 할지 정하거나 고른 것 (예: "나라면 도와줄 거야")
- REASON: 이유·까닭을 말한 것 (예: "왜냐하면 불쌍하니까")
- PERSPECTIVE: 다른 인물 입장에서 본 것
- SOLUTION: 문제를 푸는 방법을 낸 것
- RESULT: 어떻게 될지 결과를 예상하거나 말한 것
- EMOTION: 자기 또는 인물의 감정을 말한 것 (감정분석은 이 요소로만 표현)
- EMPATHY: 인물의 마음에 공감한 것
- REQUEST: 부탁·요청을 표현한 것

[유효성 판정 규칙 — 하나만 선택]
- VALID: 질문과 관련된 의미 있는 답
- SHORT: 단답·2어절 이하 (예: "응", "몰라")
- UNCLEAR: 의미 파악 불가 (STT 오인식 의심 포함)
- OFF_TOPIC: 질문과 무관한 이야기
- PLAYFUL: 의성어·장난 발화 (예: "뿡뿡", 노래 부르기)

[규칙]
1. evidence는 아이 발화에서 반드시 그대로 인용한다. 없는 말을 만들지 않는다.
2. 애매한 요소는 넣지 않는다. 빈 배열이어도 된다.
3. 아이 말투가 비문·부정확해도 의도를 최대한 해석하되, 해석 불가면 UNCLEAR.
4. JSON 외 다른 텍스트를 출력하지 않는다.
```

### 유저 프롬프트 뼈대 (턴마다 가변)

```
[이야기] {story_title} / 장면: {scene_summary}
[캐릭터 질문] {last_character_question}
[아이 발화] {text}
```

**운영 메모**: 시스템 프롬프트는 고정(캐싱 대상), 가변부는 유저 프롬프트로 분리. STT 게이트 신호(낮은 confidence)가 있는 턴은 분석 결과가 UNCLEAR로 나오기 쉬우므로, 서버 규칙의 저정보 연속 판정과 자연스럽게 연결된다.

---

## 6. 최종 권장안

> **판단 = 서버 코드(필수 전제), 시작 = gpt-4o-mini 단일 통합(프롬프트 2종 분리, 역할별 model은 설정값으로 분리).**
> **리허설에서 캐릭터 질문 품질이 부족하면 생성만 gpt-4o로 스위치(하이브리드) — 설정 한 줄 변경.**

### 근거

1. **개발 단순성이 해커톤 제1 제약**: 통합안은 모델 1개·키 1개·오류 처리 1벌. STT가 이미 Whisper API(OpenAI)이므로 **키·SDK·과금 계정이 재사용**되어 벤더 추가 비용(Claude·Gemini 키 발급, SDK 추가)이 0이다.
2. **비용 차이는 의사결정 요인이 아님**: 데모 500턴 기준 통합 ≈ $0.3, 분리 ≈ $2.7 — 어느 쪽도 무시 가능. 따라서 비용이 아니라 품질·단순성으로 결정하는 것이 맞고, 그 균형점이 "통합으로 시작, 생성만 승격 가능"이다.
3. **분석 역할은 소형 모델로 충분**: 판단(부족 계산·모드·종료)이 서버 규칙으로 빠져 있어 LLM 부담이 "관찰→JSON 보고"로 낮고, structured output이 스키마를 강제하므로 소형 모델의 최대 약점(출력 형식 불안정)이 제거된다.
4. **생성 품질 리스크는 스위치로 헤지**: 서버가 부족 요소를 지정해 주는 강한 제약 프롬프트라서 mini도 데모 수준은 가능하되, 페르소나 톤이 아쉬우면 gpt-4o(또는 Claude Sonnet 5 — 인트로가 $2/$10로 gpt-4o와 동가)로 생성만 교체. 역할별 model 설정 분리가 이 전환을 한 줄로 만든다.
5. **아동 안전은 모델 선택이 아니라 파이프라인으로**: 생성 프롬프트에 안전 가드(연령 적합 어휘·금지 주제) + 서버 후검증(길이 상한·금칙어 필터) + CLOSING 고정 대사 폴백. 어떤 모델을 쓰든 공통으로 필요하므로 모델 결정과 독립.
6. **오픈소스 자체 서버는 후순위가 타당**: LLM 입력은 텍스트뿐이라 프라이버시상 자체 서버를 강제할 사유가 없다. JSON 안정성·운영 부담을 감수할 이유가 해커톤 범위엔 없음(자료 판단과 일치).
7. **지연시간**: 턴 파이프라인이 분석→규칙→생성→TTS 순차이므로 소형 모델의 1초 미만 응답이 체감 품질에 직결 — 통합안(전부 소형)이 가장 빠르고, 분리안도 생성 +1초 이내로 수용 가능.

### 실행 체크리스트

1. LLM 클라이언트 1개 + 역할별 설정(`ANALYSIS_MODEL=gpt-4o-mini`, `GENERATION_MODEL=gpt-4o-mini`) 분리
2. 분석 호출: 위 스키마로 structured output 강제, 결과를 `utterance_analyses` 저장
3. 서버 규칙 모듈: missing 계산·턴 카운트·모드 결정 순수 함수로 구현(단위 테스트 가능)
4. 생성 호출: 페르소나+부족요소 1~2개+최근 대화 → 후검증(길이·금칙어) → TTS
5. 리허설 10세션: 캐릭터 질문 품질 평가 → 미달 시 `GENERATION_MODEL=gpt-4o` 스위치
6. 제출 전 요금 재확인 (OpenAI/Gemini 수치는 개략치, Claude는 2026-06 기준 확인 값 — Sonnet 5 인트로가는 2026-08-31 종료)

---

## 부록. "AI를 2개로 합치면 관리가 줄지 않나?" 검토 (STT+LLM 통합안)

> 검토 배경: "Whisper에게 분석·질문생성까지 맡겨 AI를 3개→2개로 줄이면 관리 부담과 서버 통신 비용이 줄지 않는가?"라는 질문에 대한 분석.

### 전제 교정 2가지

1. **Whisper는 STT 전용 모델이다.** 음성→텍스트 변환만 가능하고 텍스트 이해·분석·생성 능력이 없어 "Whisper로 합치기"는 성립하지 않는다. 실제로 가능한 통합은 **오디오 입력을 직접 받는 멀티모달 LLM**(gpt-4o-audio 계열)에게 "음성을 듣고 전사+분석까지 한 번에" 시키는 구성이다.
2. **현재 설계도 서버 여러 개가 아니다.** AI는 3개(Whisper·분석·생성)처럼 보이지만 운영 형태는 **벤더 1개(OpenAI)·API 키 1개·백엔드 서버 1개**에서 발화당 API 2~3회 호출이다(분석·생성은 통합안이면 같은 모델에 프롬프트만 2개). 합쳐서 줄어드는 것은 프로젝트/서버가 아니라 **발화당 API 왕복 1회**뿐이다.

### 비교: 역할 분리 3콜 vs 멀티모달 통합 2콜

| 축 | **분리** (Whisper → 분석 LLM → 생성 LLM) ★현재 권장 | **합침** (오디오 LLM이 STT+분석 → 생성 LLM) |
|---|---|---|
| 관리 부담 | 벤더 1개·키 1개는 동일. 호출 경로 3개 | 호출 경로 2개. 단, 오디오 프롬프트라는 새 튜닝 영역이 생겨 실질 부담은 비슷하거나 증가 |
| 지연시간 | 발화당 왕복 3회 | 왕복 1회 절약 → 체감 0.5~1초 단축 (통합의 실질적 장점) |
| 비용 | Whisper $0.006/분 + 텍스트 토큰(미미) | 오디오 입력 토큰 단가가 Whisper 대비 대략 수 배~10배 수준(요금표 확인 필요) → 오히려 비싸질 가능성 |
| STT 정확도 통제 | `prompt` 힌팅·`no_speech_prob` 게이트·후처리 등 **stt_analysis.md 확정 파이프라인 그대로 재사용** | 전사 confidence 신호가 없어 **실패 게이트("다시 말해줄래?") 구현 곤란**. 힌팅도 제한적 |
| DB 설계 정합 | `stt_raw_text`(원문)/`text`(확정) 분리 저장이 자연스러움 | 전사를 함께 출력시켜야 하는데 원문 보존 신뢰도 낮음 |
| 디버깅·시연 안정성 | 이상 발화 시 **STT 오인식인지 분석 오류인지 단계별 분리 진단 가능** | 한 덩어리라 원인 분리 불가. 실패 시 오디오 포함 전체 재호출(비용·지연 이중 손해) |
| 아키텍처 원칙 | 판단=서버 규칙이 분석과 생성 **사이에** 들어가는 §4 구조와 정합 | STT+분석까지만 합치면 유지 가능. **생성까지 1콜로 합치면 서버 규칙이 끼어들 자리가 없어져 "판단=코드" 원칙 위반** |

분석+생성을 1콜로 합치는 안은 모델 성능 이전에 구조 문제다: 서버 규칙이 분석 결과로 missing_elements와 모드(NORMAL/GUIDED/CLOSING)를 계산한 **다음** 생성이 실행되어야 하는데, 한 번에 합치면 그 판단을 LLM이 하게 되어 `llm_context.md`의 명시 원칙("판단은 서버 규칙, LLM은 분석·생성만")을 위반한다.

### 결론

통합으로 얻는 것은 사실상 **왕복 1회(0.5~1초)**뿐이고, 잃는 것은 STT 실패 게이트·원문 보존·단계별 디버깅·기존 파이프라인 재사용이다. 비용도 오디오 토큰 단가 때문에 불리할 공산이 크다. 기대했던 "관리 단순화"는 이미 단일 벤더+단일 키+분석/생성 모델 통합(gpt-4o-mini)으로 달성되어 있으므로 **현재의 분리 구조(Whisper 1콜 + LLM 2콜) 유지를 권장**한다. 지연이 실측으로 문제가 확인되면 그때 gpt-4o-audio 계열로 STT+분석만 합치는 실험을 하는 것이 순서다.
