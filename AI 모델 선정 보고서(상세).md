# 굿퀘스천 AI 기술 선정 보고서 — 선정 과정 포함 상세본

프로젝트: **굿퀘스천** — 아이가 동화 캐릭터와 음성으로 대화하며 생각하는 힘을 기르는 아동 학습 서비스 작성일: 2026-08-05

---

## 한눈에 보는 최종 선정 결과

| 영역 | 하는 일 | 선정 결과 | 핵심 선정 이유 |
| --- | --- | --- | --- |
| **음성 인식 (STT)** | 아이의 말을 글자로 변환 | **OpenAI Whisper API** | 태블릿 녹음 포맷을 변환 없이 그대로 처리, 안정적인 한국어 인식, 간단하고 검증된 연동 방식 |
| **대화 AI (LLM)** | 아이의 답을 분석하고 캐릭터의 다음 질문을 생성 | **OpenAI gpt-4o-mini** (필요 시 질문 생성만 상위 모델로 전환 가능) | 분석 결과를 정해진 형식으로 안정적으로 출력, 빠른 응답 속도, 음성 인식과 동일 벤더로 운영 단순화 |
| **음성 합성 (TTS)** | 캐릭터 목소리로 대사를 읽어줌 | **타입캐스트 API** | 한국어 캐릭터·연기 톤 보이스가 가장 풍부, 캐릭터별 목소리 구분이 쉬움, 한국어 문서와 국내 서비스 환경 |

---

## 먼저 알아두면 좋은 용어

- **STT (Speech-to-Text)**: 음성을 글자로 바꾸는 기술. "음성 인식"
- **TTS (Text-to-Speech)**: 글자를 음성으로 읽어주는 기술. "음성 합성"
- **LLM (대규모 언어 모델)**: 글을 이해하고 생성하는 AI. ChatGPT에 쓰이는 종류의 기술
- **API**: 외부 서비스의 기능을 우리 서버에서 호출해 쓰는 연결 방식
- **신뢰도 점수**: AI가 "내 결과가 얼마나 확실한지"를 함께 알려주는 수치. 결과를 믿을지 말지 판단하는 근거로 사용
- **벤더**: AI 기능을 제공하는 회사(OpenAI, Google, 네이버 등)

---

## 공통 선정 기준

세 영역 모두 아래 다섯 가지 축으로 후보를 평가했습니다.

1. **한국어·아동 적합성** — 아이의 부정확한 발음, 짧은 문장, 장난 발화까지 다뤄야 함
2. **연동 안정성** — iPad·안드로이드 태블릿 웹앱에서 추가 변환·절차 없이 동작하는가
3. **응답 속도** — 아이가 말한 뒤 캐릭터가 답하기까지의 체감 시간
4. **운영 단순성** — 계정·연동 방식의 수, 장애 지점의 수
5. **교체 가능한 구조** — 특정 서비스에 종속되지 않고, 더 나은 대안이 나오면 설정 수준으로 전환 가능한가

도입·운영 비용도 함께 비교했으나, 검토 결과 후보 간 비용 차이가 의사결정을 좌우할 규모가 아니어서 **품질과 안정성 기준으로 판단**했습니다. (각 비교표에 참고용으로 병기)

---

## 1. 음성 인식 (STT)

### 과제

아이가 캐릭터의 질문에 음성으로 답하면 글자로 변환해 화면에 보여주고 분석 입력으로 사용합니다. 어려운 점은 **아이 특유의 발화**입니다. 발음이 부정확하고, 문장이 짧고, 장난스러운 말("뿡뿡" 등)이 섞이며, 태블릿 내장 마이크로 녹음되어 잡음도 있습니다. 스트리밍(실시간 자막)은 필요 없고, 발화가 끝난 뒤 한 번에 변환하는 방식이면 충분합니다.

### 검토 과정 — 후보 비교

서비스 설계상 원본 음성은 저장하지 않고 STT로 변환된 텍스트만 처리·저장합니다(오디오는 변환 후 즉시 폐기).

| 후보 | 한국어·아동 발화 | 신뢰도 확인·힌트 기능 | 태블릿 웹앱 연동 | 비용 (시연 규모 약 83분 기준) |
| --- | --- | --- | --- | --- |
| **OpenAI Whisper API** ⭐선정 | 다국어 대량 학습으로 한국어 준수. 단, 짧은 발화·무음 구간에서 없는 말을 만들어내는 경향 → 보완 장치 필수 | 공식 신뢰도 점수는 없으나 내부 지표(무음 확률, 로그 확률)를 신뢰도 판단에 활용 가능. 캐릭터명·핵심 단어 힌트 전달 지원 | **가장 쉬움** — 요청 1회로 완료. iPad(audio/mp4)·안드로이드(webm/opus) 녹음 포맷을 변환 없이 모두 수용 | 약 $0.5 |
| OpenAI gpt-4o-mini-transcribe | Whisper 대비 인식률 개선 경향, 환각 주의점은 유사 | 로그 확률 요청 가능, 힌트 지원 | Whisper와 동일 계열로 동일하게 쉬움 | 약 $0.25 |
| Google Cloud STT | **한국어에 강함.** 짧은 발화용 모델 선택 가능, 무음 환각이 Whisper보다 적음 | **공식 신뢰도 점수 제공(후보 중 최고).** 힌트 단어별 가중치 지정까지 가능 | 중간 — 인코딩 명시 필요. iPad 녹음 포맷은 서버 측 변환 단계가 추가로 필요할 수 있음 | 월 60분 무료, 초과분 약 $0.4\~0.6 |
| 네이버 클로바 스피치 | 국내 발음 특화 — 아동 발음 변이에 유리할 가능성 | 일부 API에서 신뢰도·키워드 부스팅 지원 | 중간 — 클라우드 계정·앱 등록 절차, 포맷 제약 확인 필요 | 종량제 + 신규 무료 크레딧 |

**판단**: Whisper API와 Google STT가 최종 후보로 압축되었습니다. Google은 공식 신뢰도 점수와 강력한 힌트 기능이 강점이지만, iPad 녹음 포맷에 서버 변환 단계가 붙을 수 있습니다. Whisper는 두 태블릿의 녹음 포맷을 변환 없이 수용해 **오류가 생길 지점 자체가 적고**, 신뢰도 판단은 내부 지표로 대체 가능합니다. 어차피 필수로 구축해야 하는 품질 보완 장치(아래 참조)가 Whisper의 약점을 상쇄하므로, 연동 안정성이 높은 Whisper를 선정했습니다.

**교체 가능 구조**: STT 호출부는 "오디오+힌트 입력 → 텍스트+신뢰도 출력" 인터페이스로 추상화해 두어, 이후 더 나은 대안이 나오면 힌트·실패 판정·교정 장치는 그대로 두고 모델 교체 수준으로 전환할 수 있습니다.

### 품질을 지키는 3단계 장치

아동 발화의 오인식은 어떤 모델을 골라도 피할 수 없으므로, **모델 선택보다 이 보완 장치가 체감 품질을 좌우**한다고 판단했습니다.

1. **변환 전 — 힌트 주입**: 세션 시작 시 현재 이야기의 캐릭터명·핵심 단어 목록을 구성해 STT 호출 시 함께 전달합니다. 발음이 부정확해도 문맥에 맞는 단어로 인식할 확률이 올라갑니다. 너무 짧은 녹음(0.5초 미만)이나 무음은 전송 자체를 차단합니다.
2. **변환 시 — 실패 판정**: 변환 결과를 순서대로 검사합니다 — ① 빈 텍스트·초단문 ② 신뢰도 지표가 기준 미달 ③ 오류 패턴(같은 어구 반복, 자막체 상투구). 하나라도 걸리면 "실패"로 처리해 잘못된 텍스트가 대화에 저장되지 않게 하고, 캐릭터가 "다시 말해줄래?"로 자연스럽게 재시도를 유도합니다. 재시도 한도(2회) 초과 시 다음 질문으로 진행해 세션이 막히지 않게 합니다.
3. **변환 후 — 원문 보존 + 가벼운 교정**: 판정을 통과한 원문은 그대로 저장하고, 소형 AI가 이야기 맥락을 참고해 **맞춤법·띄어쓰기·조사만** 교정합니다. 의미·어휘 변경과 내용 창작은 금지 규칙으로 막고, 확신이 없으면 원문을 유지합니다. 원문과 교정본을 분리 저장해 문제 발생 시 언제든 복구할 수 있습니다.

---

## 2. 대화 AI (LLM)

### 과제와 역할 분해

대화 AI는 두 가지 일을 합니다.

- **발화 분석**: 아이의 답에서 사고 요소(결정, 이유, 다른 사람 입장, 해결 방법, 결과 예상, 감정, 공감, 부탁)를 찾아내고, 답의 유형(유효한 답 / 단답 / 불분명 / 주제 이탈 / 장난)을 분류
- **캐릭터 질문 생성**: 아직 나오지 않은 사고 요소를 자연스럽게 끌어내는 캐릭터의 다음 질문 생성

### 설계 원칙 — "판단은 프로그램이, AI는 분석과 생성만"

검토 초기부터 확정한 원칙입니다. 대화의 진행 결정(부족 요소 계산, 턴 수 관리, 계속/마무리 결정)은 AI가 아니라 **서버 프로그램(코드)** 이 담당합니다.

- AI ①(분석)은 "이 답에서 무엇이 관찰되었는가"만 보고 — 부족 요소 판단은 하지 않음
- 서버 프로그램이 관찰 결과를 누적해 부족 요소와 대화 모드(일반/유도/마무리)를 계산
- AI ②(생성)는 서버가 지정한 부족 요소만 유도하는 질문을 생성 — 대화 종료를 스스로 판단하지 않음

**이유**: 진행 로직이 코드에 있으면 대화 흐름이 항상 재현 가능하고, 문제 발생 시 원인 단계를 바로 짚을 수 있으며, AI 호출이 실패해도 재시도·대체 흐름을 프로그램이 제어할 수 있습니다.

### 검토 과정 1 — 모델 하나로 통합할까, 역할별로 나눌까?

| 비교 축 | **통합안** — gpt-4o-mini 하나로 분석·생성 모두 (프롬프트만 2종) | **분리안** — 분석은 소형, 생성은 중급(gpt-4o) |
| --- | --- | --- |
| 품질 | 분석은 소형으로 충분(출력 형식 강제 기능이 안정). 캐릭터 질문의 연기력이 다소 밋밋할 수 있으나, 서버가 유도 요소를 지정하는 강한 제약 덕에 격차 축소 | 캐릭터 말투 유지·문장 자연스러움 우위 |
| 응답 속도 | 모두 소형이라 가장 빠름 (턴 파이프라인에 유리) | 생성 단계 +0.5\~1초 |
| 운영 | 모델 1개, 오류 처리 1벌 — 가장 단순 | 호출 경로 2개 (같은 벤더라 실질 차이는 작음) |
| 비용 (시연 500턴 기준) | 약 $0.3 | 약 $2.7 |

**판단**: 두 안의 비용 차이는 의사결정 요인이 아니었고, 품질과 단순성의 균형으로 결정했습니다. 결론은 **절충안** — 역할별 모델을 설정값으로 분리해 두고, **통합(mini)으로 시작하되 리허설에서 질문 품질이 부족하면 생성만 상위 모델로 전환**합니다. 두 안의 차이를 "설정 한 줄"로 만들어 두는 것이 핵심입니다.

### 검토 과정 2 — 모델 후보 비교

**발화 분석용** (정해진 JSON 형식을 안정적으로 출력하는 것이 최우선)

| 모델 | 특징 | 판단 |
| --- | --- | --- |
| **gpt-4o-mini** ⭐선정 | 출력 형식 강제 기능 안정, 응답 빠름, Whisper와 동일 벤더 | 필요 조건 모두 충족 |
| gpt-4.1-mini | 지시 이행력 소폭 우위 | 품질 격차가 유의미하지 않음 |
| Claude Haiku 4.5 | 형식 강제 지원, 한국어 준수 | 품질 동급이나 벤더 추가 필요 |
| Gemini Flash | 형식 강제 지원 | 품질 동급이나 벤더 추가 필요 |

**캐릭터 질문 생성용** (캐릭터 말투와 아동 적합 표현이 최우선)

| 모델 | 특징 | 판단 |
| --- | --- | --- |
| **gpt-4o-mini** ⭐기본 | 서버가 유도 요소를 지정하는 구조라 소형으로도 일관된 품질 가능 | 기본값 |
| gpt-4o | 말투 자연스러움·연기력 우위, 분석과 동일 벤더 | 품질 미달 시 전환 대상 1순위 |
| Claude Sonnet 5 | 캐릭터 유지·안전성 강점 | 품질 동급이나 벤더 추가 필요 |
| Gemini Pro | 동급 품질 | 벤더 추가 필요 |

벤더 선택의 공통 논리: STT가 이미 OpenAI(Whisper)이므로 같은 벤더를 쓰면 **계정·연동 방식·오류 처리를 재사용**할 수 있습니다. 타 벤더 모델들은 품질이 동급이어서, 벤더를 추가할 만큼의 이점이 없었습니다.

### 검토 과정 3 — "AI를 3개에서 2개로 줄이면 관리가 단순해지지 않나?"

STT와 발화 분석을 하나로 합치는 안(음성을 직접 이해하는 멀티모달 AI에게 "듣고 → 글자화 + 분석까지 한 번에" 시키는 구성)도 검토했습니다. **기각**했으며, 이유는 다음과 같습니다.

| 비교 축 | 분리 유지 (현재 구조) | 통합 (음성 이해 AI) |
| --- | --- | --- |
| 실제로 줄어드는 것 | — | 발화당 API 왕복 1회 (체감 0.5\~1초 단축) |
| 오인식 대응 | 신뢰도 판정으로 "다시 말해줄래?" 재시도 유도 가능 | 전사 신뢰도 신호가 없어 **실패 판정 구현 곤란** |
| 원문 보존 | 인식 원문과 교정본 분리 저장 | 원문 보존 신뢰도 낮음 |
| 문제 진단 | 이상 발화 시 인식 오류인지 분석 오류인지 **단계별 분리 진단 가능** | 한 덩어리라 원인 분리 불가 |
| 설계 원칙 정합 | 분석 결과 → 서버 판단 → 생성 순서 유지 | 생성까지 합치면 서버 판단이 끼어들 자리가 없어져 "판단은 코드" 원칙 위반 |

관리 부담 측면에서도, 현재 구조는 이미 **벤더 1개·계정 1개·서버 1대**로 운영되므로 통합해도 줄어드는 것은 API 왕복 1회뿐입니다. 얻는 것(약간의 속도)보다 잃는 것(실패 판정, 원문 보존, 단계별 진단)이 크다고 판단했습니다. 응답 지연이 실측에서 문제로 확인되면 그때 부분 통합을 실험하는 것으로 순서를 정했습니다.

### 아동 안전 장치

모델 선택과 독립적으로 공통 적용합니다.

- 질문 생성 프롬프트에 연령 적합 어휘·금지 주제 규칙 명시
- 생성 결과를 서버가 재검증 (길이 상한, 금칙어 필터)
- 대화 마무리 멘트는 사전 확정한 고정 대사만 사용 (AI가 즉석 생성하지 않음)

---

## 3. 캐릭터 음성 (TTS)

### 과제

방귀쟁이 며느리, 시아버지, 마을 이장 등 **여러 캐릭터가 서로 다른 목소리**로 말해야 하며, 어린이 청자에게 자연스러운 한국어 발음·억양이 필요합니다. 대사는 두 종류입니다 — **고정 대사**(장면 시작·마무리 멘트, 사전 확정)와 **가변 질문**(AI가 실시간 생성).

### 검토 과정 1 — 글로벌 서비스에서 한국 서비스로 방향 전환

1차 검토는 글로벌 서비스(ElevenLabs, Google Cloud TTS, Azure, 오픈소스 XTTS-v2) 대상으로 진행해 ElevenLabs를 잠정 선정했습니다. 그러나 검증 과정에서 글로벌 서비스의 공통 한계가 확인되어 방향을 전환했습니다.

- **한국어 억양의 부자연스러움**: 다국어 범용 모델 특성상 어린이 대상 서비스에 필요한 자연스러운 낭독 품질에 미달
- **캐릭터 연기 톤 부족**: 전래동화 캐릭터(코믹한 며느리, 노년 시아버지 등)에 맞는 목소리 선택지가 빈약
- 영어 문서·해외 결제 등 도입 마찰

이에 따라 2차 검토부터 **한국어 특화 국내 서비스** 기준으로 후보를 재선정했습니다.

캐릭터 목소리는 **기성 TTS 보이스를 캐릭터별로 배정**하는 방식으로 확정했습니다(성우 녹음 데이터 없음, 2026-08-05 확인).

### 검토 과정 2 — 국내 서비스 후보 비교

| 구분 | **타입캐스트 API** ⭐선정 | 수퍼톤 API | 클로바 보이스 API | 클로바더빙 (웹 도구) |
| --- | --- | --- | --- | --- |
| 보이스 | 한국어 캐릭터·연기 보이스 수백 종 (아동·노인·사극 톤 포함), 감정·스타일 조절 | 감정 표현 특화, 목소리 복제 기능 보유 | 페르소나 보이스 100여 종 (아동 보이스 포함) | 웹에서 수동 생성·다운로드 (API 아님) |
| 캐릭터 구분 | 캐릭터별 프리셋 배정만으로 완결 | 라이브러리에서 배정 | 페르소나 배정 | 보이스 선택 후 파일 저장 |
| 연동 | 낮음 — 표준 API + 한국어 문서 | 낮음 — 키 즉시 발급 | 중간 — 클라우드 계정·인증 절차 필요 | 통합 불가 (파일만 산출) |
| 무료 제공 | **월 3만 자 (후보 중 최대)** | 월 약 1만 자 상당 | 무료 티어 없음 | 월 1.5만 자, 출처 표기 필수 |
| 판단 | 연기 톤 최다 + 무료 한도 최대 | 클로닝 강점은 현 시점 불필요 | 품질 우수하나 도입 절차 김 | 실시간 질문 대응 불가 → 고정 대사 배치 생성 보조 수단으로만 |

### 최종 선정과 이유

**타입캐스트 API** — 전래동화 캐릭터에 맞는 연기 톤 보이스가 가장 풍부해 별도 제작 없이 프리셋 배정만으로 캐릭터 목소리를 완성할 수 있고, 한국 기업 서비스로 한국어 발음·억양 품질과 국내 사용 환경(한국어 문서, 국내 서버)이 검증되어 있습니다. 현재 사용량 전망으로는 무료 제공 범위 내 운영이 가능하며, 초과 시 유료 플랜으로 전환합니다.

### 작동 방식

- **고정 대사**: 사전에 전량 음성 파일로 생성해 정적 저장소에 배치 → 재생 시 API 호출 없이 즉시 재생
- **가변 질문**: 실시간 합성하되, 같은 문장이 반복 생성되는 경우를 대비해 "캐릭터+문장" 단위로 캐싱해 재사용
- **키 보안**: TTS API 키는 브라우저에 노출하지 않고 백엔드 프록시를 경유
- **iPad 대응**: iOS Safari의 자동재생 제한을 세션 시작 버튼 터치 시점에 해제
- **교체 가능 구조**: 합성 요청을 "텍스트+보이스 → 오디오" 인터페이스 하나로 추상화해, 더 나은 대안이 나오면 **앱·서버 코드 변경 없이** 설정 교체 수준으로 전환

---

## 전체 흐름 — 한 번의 대화 턴이 처리되는 과정

```
아이가 말하고 '보내기'를 터치
   1. [STT] 음성을 글자로 변환 (힌트 주입 → 실패 판정 → 교정)
   2. [AI 분석] 답에서 사고 요소·답변 유형을 찾아 보고
   3. [서버 프로그램] 부족한 요소 계산, 대화 계속/마무리 결정
   4-a. 계속  → [AI 생성] 부족 요소를 유도하는 캐릭터 질문 생성
              → 서버 검증 → [TTS] 캐릭터 목소리로 합성 → 재생
   4-b. 마무리 → 사전 제작된 고정 마무리 대사 재생
```

각 단계가 분리되어 있어 문제가 생기면 어느 단계의 문제인지 바로 확인할 수 있고, 특정 단계의 서비스를 교체해도 나머지는 영향을 받지 않습니다.

---

## 남은 확인 사항

- STT 실패 판정의 기준값(신뢰도 임계치)은 실제 시연 태블릿과 아이 목소리 샘플 10\~20건으로 실측한 뒤 확정
- 리허설(10세션)에서 캐릭터 질문 품질 평가 → 미달 시 생성 모델을 gpt-4o로 전환
- TTS 사용량 실측(10세션) → 시연일까지 사용량 재추정, 무료 한도 초과 예상 시 유료 플랜 전환
- 각 서비스의 요금·제공 조건은 변동될 수 있어 제출 전 공식 페이지에서 재확인