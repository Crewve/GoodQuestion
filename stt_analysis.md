# 아이 발화 STT 모델 선정 분석 보고서

프로젝트: **굿퀘스천** — 아동 음성대화 학습 서비스 (해커톤 MVP) 작성일: 2026-08-05 · 근거 문서: `stt_context.md`

---

## 1. 전제 요약

- **용도**: 아이가 캐릭터 질문에 음성으로 답 → STT 변환 → 화면 표시 + 세션 저장 + 발화 분석 입력.
- **요건**: 한국어 아동 발화(부정확한 발음·짧은 문장·비문·PLAYFUL 장난 발화), 발화 단위 **배치 변환**(스트리밍 불필요), 태블릿 웹앱(iPad·안드로이드) 내장 마이크, 변환 실패 시 메시지 미생성(실패 게이트), `stt_raw_text`/`text` 분리 저장(후처리 여지).
- **제약**: 해커톤 제출용 — 최소비용·최소개발 우선. 원본 음성 미저장·STT 변환 텍스트만 처리/저장(기획 문서 명시) — 오디오는 변환 후 즉시 폐기.
- **비용 산정 시나리오**: 데모 규모 기준 발화 평균 10초 × 500회 ≈ **83분** 가정.

---

## 2. 후보 비교

| 후보 모델 | 예상 비용 (무료 티어 포함) | 아동 발화 강건성 | confidence / 힌팅 | 태블릿 웹앱 통합 난이도 |
| --- | --- | --- | --- | --- |
| **OpenAI Whisper API** (`whisper-1`) ★추천 | $0.006/분 → 83분 ≈ **$0.5**. 무료 티어 없음(과금 미미) | 다국어 대량 학습으로 한국어 준수. 단, **짧은 발화·무음 구간 환각** 경향 → 게이트·후처리 보완 필수 | 공식 confidence 없음. `verbose_json`의 `avg_logprob`·`no_speech_prob`를 **유사 confidence로 게이트에 활용 가능**. 힌팅: `prompt` 파라미터(≤224 토큰)에 캐릭터명·핵심단어 주입 | **최하(가장 쉬움)**: multipart POST 1회. iPad Safari(`audio/mp4`/AAC)·안드로이드 Chrome(`webm/opus`) 녹음 포맷 **둘 다 무변환 수용** |
| OpenAI `gpt-4o-mini-transcribe` | 약 $0.003/분 → 83분 ≈ **$0.25**. 무료 티어 없음 | whisper-1 대비 WER 개선 경향, 환각 주의점은 유사 | segment 정보 대신 `logprobs` 요청 가능(게이트 활용 여지). 힌팅: `prompt` 지원 | whisper-1과 동일한 엔드포인트 계열 — **최하** |
| **Google Cloud STT** (short 계열 모델) | **60분/월 무료**, 초과분 약 $0.016\~0.024/분 → 83분 ≈ **$0.4\~0.6** (신규 $300 크레딧 적용 시 사실상 0원) | **한국어 강함**. 짧은 발화용 모델 선택 가능, 무음 환각이 Whisper 계열보다 적음 | **정식 confidence 제공(최대 강점)** — 실패 게이트 구현이 가장 깔끔. 힌팅: `speech_contexts`/adaptation **boost 값까지 지정 가능(최강)** | 중: 인코딩·샘플레이트 명시 필요. `webm/opus`는 직접 지원되나 **iPad Safari의** `audio/mp4`**(AAC)는 서버 측 ffmpeg 변환 단계가 필요할 수 있음** |
| 네이버 클로바 스피치 (CSR/Clova Speech) | 종량제(대략 15초당 수 원 수준, 최신 요금은 콘솔 확인 필요) + **신규 무료 크레딧** | **국내 발음 특화** — 한국어 아동 발음 변이에 유리할 가능성 | 장문(Clova Speech) API는 confidence·키워드 부스팅 지원, 단문 CSR은 제한적 | 중: 국내 클라우드 계정·앱 등록 절차, 포맷 제약 확인 필요 — 해커톤 시간 예산에서 불리 |

**판단**: 통합 속도(포맷 무변환·단일 요청)가 압도적인 **Whisper API가 기본**. confidence 기반 게이트의 품질을 높이고 싶고 GCP 셋업 시간이 있다면 **Google STT 무료 티어가 대등한 대안**(60분/월이면 데모 대부분 커버).

---

## 3. 정확도 보정 전략 (모델 무관 공통 — 필수 파이프라인)

아동 발화 WER 상승은 어떤 모델을 골라도 피할 수 없으므로, 모델 선택보다 아래 3단계 보정 파이프라인이 체감 품질을 좌우함.

### 1단계 — 사전: 커스텀 어휘·프롬프트 힌팅

1. 세션 시작 시 현재 스토리의 **캐릭터명·핵심단어 리스트**를 서버가 구성한다.
2. STT 호출 시 주입한다:
   - Whisper API: `prompt`에 "다음은 아이가 ○○(캐릭터) 이야기에 대해 말한 내용입니다: 캐릭터명, 핵심단어…" 형식으로 삽입(224토큰 한도 내).
   - Google STT(대안 채택 시): `speech_contexts`(adaptation)에 phrase + boost 값 지정.
3. 클라이언트 사전 게이트: 녹음 길이 최소치(예: 0.5초 미만 폐기)·무음 감지(RMS 에너지)로 **빈 오디오는 전송 자체를 차단**해 비용·환각을 줄인다.

### 2단계 — 변환 시점: 신뢰도 실패 게이트

1. 변환 결과에 대해 순서대로 검사한다:
   - **빈 텍스트/초단문**(공백 제거 후 1\~2자) → 실패.
   - **confidence 임계치**: Google은 `confidence < 임계(예: 0.6)`, Whisper 계열은 `no_speech_prob > 임계(예: 0.6)` 또는 `avg_logprob < 임계(예: -1.0)` → 실패. 임계값은 데모 기기로 실측 후 조정.
   - **환각 패턴 감지**: 동일 어구 반복(n-gram 반복), 자막체 상투구("시청해주셔서 감사합니다" 류) → 실패.
2. 실패 시 **메시지를 생성하지 않고**(`messages` 미삽입 — DB 설계와 일치) 캐릭터가 "다시 말해줄래?"로 재시도를 유도한다.
3. 재시도 횟수 제한(예: 2회) 초과 시 텍스트 입력 폴백 또는 다음 질문으로 진행해 세션이 막히지 않게 한다.

### 3단계 — 사후: 원문 보존 + 경량 LLM 후처리

1. 게이트를 통과한 원문을 `messages.stt_raw_text`에 **그대로 저장**한다(교정 실패·오교정 시 복구 근거).
2. 경량 LLM(예: gpt-4o-mini급)에 스토리 컨텍스트 + 직전 질문을 주고 **맞춤법·띄어쓰기·조사만 교정**시킨다. 규칙: 의미·어휘 변경 금지, 아이가 말한 내용 창작 금지, 확신 없으면 원문 유지.
3. 교정 결과를 `messages.text`(확정 텍스트)에 저장한다. 후처리 호출 실패 시 `text = stt_raw_text` 폴백.
4. 발화 유효성 분류(VALID/SHORT/UNCLEAR/OFF_TOPIC/PLAYFUL)는 \*\*교정 후 텍스트 + 게이트 신호(confidence·길이)\*\*를 입력으로 수행해 분석 정확도를 높인다.

---

## 4. 최종 권장안

> **Whisper API(**`whisper-1`**, 여유 시** `gpt-4o-mini-transcribe` **비교) + 힌팅 + 실패 게이트 + LLM 후처리.** (2026-08-06 확정)

### 근거

1. **통합 속도가 해커톤의 제1 제약**: Whisper API는 multipart POST 1회로 끝나고, iPad Safari(`audio/mp4`)와 안드로이드 Chrome(`webm/opus`) 녹음 포맷을 변환 없이 모두 수용한다. Google STT는 iPad 포맷에 서버 변환 단계가 붙을 수 있고, 클로바는 계정·등록 절차 비용이 있다.
2. **비용은 사실상 무시 가능**: 데모 규모(≈83분)에서 Whisper $0.5 수준. 무료 티어가 꼭 필요하면 Google STT 60분/월로 대체 가능하나, 그 차액보다 통합 시간 절약의 가치가 크다.
3. **Whisper의 약점은 파이프라인으로 상쇄됨**: 공식 confidence 부재와 무음 환각은 `no_speech_prob`/`avg_logprob` 게이트 + 환각 패턴 필터 + LLM 후처리로 보완되며, 이 보정 파이프라인은 요건상 어차피 필수다(실패 게이트·`stt_raw_text` 분리 저장이 이미 설계에 존재).
4. **교체 가능 구조**: STT 호출부를 어댑터 인터페이스(입력: 오디오+힌트 리스트 / 출력: 텍스트+신뢰도 신호)로 추상화해, 더 나은 대안이 나오면 **모델 교체 수준으로 전환**할 수 있고 힌팅·게이트·후처리는 그대로 재사용된다.

### 남은 확인 사항

- confidence·`no_speech_prob` 임계값은 실제 데모 태블릿 + 아이 목소리(또는 유사 샘플)로 10\~20건 실측 후 확정할 것.
- 클로바 스피치를 쓰게 될 경우 최신 요금·무료 크레딧 조건을 네이버 클라우드 콘솔에서 재확인할 것(본 문서 수치는 개략치).