# 굿퀘스천 AI 기술 선정 보고서 (통합본)

프로젝트: **굿퀘스천** — 아이가 동화 캐릭터와 음성으로 대화하며 생각하는 힘을 기르는 아동 학습 서비스
작성일: 2026-08-05

---

## 한눈에 보는 최종 선정 결과

굿퀘스천은 세 가지 AI 기술을 사용합니다. 각 영역의 최종 선정 결과는 다음과 같습니다.

| 영역 | 하는 일 | 선정 결과 | 핵심 선정 이유 |
|---|---|---|---|
| **음성 인식 (STT)** | 아이의 말을 글자로 변환 | **OpenAI Whisper API** | 태블릿 녹음 포맷을 변환 없이 그대로 처리, 안정적인 한국어 인식, 간단하고 검증된 연동 방식 |
| **대화 AI (LLM)** | 아이의 답을 분석하고 캐릭터의 다음 질문을 생성 | **OpenAI gpt-4o-mini** (필요 시 질문 생성만 상위 모델로 전환 가능) | 분석 결과를 정해진 형식으로 안정적으로 출력, 빠른 응답 속도, 음성 인식과 동일 벤더로 운영 단순화 |
| **음성 합성 (TTS)** | 캐릭터 목소리로 대사를 읽어줌 | **타입캐스트 API** | 한국어 캐릭터·연기 톤 보이스가 가장 풍부, 캐릭터별 목소리 구분이 쉬움, 한국어 문서와 국내 서비스 환경 |

세 영역 모두 **특정 서비스에 종속되지 않는 구조**로 설계되어, 이후 더 나은 대안이 나오면 설정 변경 수준으로 교체할 수 있습니다.

---

## 먼저 알아두면 좋은 용어

- **STT (Speech-to-Text)**: 음성을 글자로 바꾸는 기술. "음성 인식"
- **TTS (Text-to-Speech)**: 글자를 음성으로 읽어주는 기술. "음성 합성"
- **LLM (대규모 언어 모델)**: 글을 이해하고 생성하는 AI. ChatGPT에 쓰이는 종류의 기술
- **API**: 외부 서비스의 기능을 우리 서버에서 호출해 쓰는 연결 방식
- **신뢰도 점수**: AI가 "내 결과가 얼마나 확실한지"를 함께 알려주는 수치. 결과를 믿을지 말지 판단하는 근거로 사용

---

## 1. 음성 인식 (STT) — 아이의 말을 글자로

### 이 영역의 과제

아이가 캐릭터의 질문에 음성으로 답하면, 이를 글자로 변환해 화면에 보여주고 분석의 입력으로 사용합니다. 어려운 점은 **아이 특유의 발화**입니다. 발음이 부정확하고, 문장이 짧고, 장난스러운 말("뿡뿡" 등)이 섞이며, 태블릿 내장 마이크로 녹음되어 잡음도 있습니다.

### 후보 비교

| 후보 | 한국어·아동 발화 적합성 | 결과 신뢰도 확인 | 태블릿 웹앱 연동 | 종합 평가 |
|---|---|---|---|---|
| **OpenAI Whisper API** ⭐선정 | 다국어 대량 학습 기반으로 한국어 인식 준수 | 내부 지표를 신뢰도 판단에 활용 가능 | **가장 쉬움** — iPad와 안드로이드의 녹음 포맷을 변환 없이 모두 수용 | 연동 안정성과 인식 품질의 균형이 가장 좋음 |
| Google Cloud STT | 한국어에 강하고 짧은 발화 처리에 유리 | 공식 신뢰도 점수 제공 (후보 중 최고) | 중간 — iPad 녹음 포맷은 서버에서 변환 과정이 추가로 필요할 수 있음 | 신뢰도 기능은 우수하나 연동 단계가 더 많음 |
| 네이버 클로바 스피치 | 국내 발음 특화로 아동 발음 변이에 유리할 가능성 | 일부 API에서 지원 | 중간 — 계정·앱 등록 절차 필요 | 잠재력은 있으나 도입 절차가 상대적으로 김 |
| 브라우저 내장 처리 (온디바이스) | 태블릿 성능 한계로 소형 모델만 가능 → 정확도 가장 낮음 | 구현 부담 큼 | 어려움 — 기기별 성능 편차, 호환성 리스크 | 아동+내장 마이크 환경에서 품질 리스크가 커서 제외 |

### 선정 이유

1. **연동이 가장 단순하고 안정적**: iPad(Safari)와 안드로이드(Chrome)는 서로 다른 형식으로 녹음하는데, Whisper API는 두 형식을 변환 없이 그대로 받습니다. 변환 단계가 없다는 것은 곧 오류가 생길 지점이 하나 줄어든다는 뜻입니다.
2. **힌트 기능으로 아동 발화 인식률 보완**: 변환 요청 시 현재 이야기의 캐릭터 이름·핵심 단어를 함께 전달하면, 발음이 부정확해도 문맥에 맞는 단어로 인식할 확률이 올라갑니다.
3. **약점은 파이프라인으로 보완 가능**: Whisper의 알려진 약점(무음 구간에서 없는 말을 만들어내는 현상)은 아래의 3단계 품질 관리 장치로 걸러집니다.

### 품질을 지키는 3단계 장치

어떤 STT를 쓰더라도 아동 발화는 오인식이 생길 수 있으므로, 모델 선택과 별개로 다음 장치를 둡니다.

1. **변환 전 — 힌트 주입**: 이야기의 캐릭터명·핵심 단어를 STT에 미리 알려줘 인식률을 높입니다. 너무 짧거나 무음인 녹음은 전송 자체를 차단합니다.
2. **변환 시 — 실패 판정**: 빈 결과, 신뢰도가 낮은 결과, 오류 패턴(같은 말 반복 등)은 "실패"로 처리하고, 캐릭터가 "다시 말해줄래?"라고 자연스럽게 재시도를 유도합니다. 잘못 인식된 텍스트가 대화에 섞이는 것을 원천 차단합니다.
3. **변환 후 — 원문 보존 + 가벼운 교정**: 인식된 원문은 그대로 저장하고, 별도의 소형 AI가 맞춤법·띄어쓰기만 교정합니다. 아이가 말한 내용을 바꾸거나 창작하는 것은 금지 규칙으로 막습니다. 원문이 항상 남아 있어 교정에 문제가 생겨도 복구할 수 있습니다.

### 확장 옵션 — 음성 데이터를 외부로 보낼 수 없게 되는 경우

개인정보 보호 요건이 강화되어 아이의 음성을 외부 서비스로 전송할 수 없게 되면, 자체 서버에서 동작하는 오픈소스 STT(**faster-whisper**)로 교체합니다. 음성이 우리 서버 밖으로 나가지 않으며, 위의 3단계 품질 장치는 그대로 재사용됩니다. STT 호출부를 교체 가능한 구조로 만들어 두었기 때문에 전환 작업은 모델 교체 수준으로 끝납니다.

---

## 2. 대화 AI (LLM) — 답을 분석하고, 다음 질문을 만든다

### 이 영역의 과제

대화 AI는 두 가지 일을 합니다.

- **발화 분석**: 아이의 답에서 어떤 사고 요소(결정, 이유, 다른 사람 입장, 해결 방법, 결과 예상, 감정, 공감, 부탁)가 드러났는지 찾아내고, 답의 유형(유효한 답 / 단답 / 불분명 / 주제 이탈 / 장난)을 분류합니다.
- **캐릭터 질문 생성**: 아직 나오지 않은 사고 요소를 자연스럽게 끌어내는 캐릭터의 다음 질문을 만듭니다.

### 핵심 설계 원칙 — "판단은 프로그램이, AI는 분석과 생성만"

대화의 진행 결정(어떤 요소가 부족한지 계산, 몇 번째 턴인지, 대화를 계속할지 마무리할지)은 AI가 아니라 **서버 프로그램(코드)** 이 담당합니다.

- AI ①(분석)은 "이 답에서 무엇이 관찰되었는지"만 보고합니다.
- 서버 프로그램이 그 결과로 부족한 요소를 계산하고 대화 모드를 결정합니다.
- AI ②(질문 생성)는 서버가 지정해 준 부족 요소만 유도하는 질문을 만듭니다.

이 구조 덕분에 **대화 흐름이 항상 예측 가능하고**, 문제가 생겼을 때 어느 단계의 문제인지 바로 짚을 수 있으며, AI 호출이 실패해도 세션이 멈추지 않도록 프로그램이 흐름을 제어합니다.

### 후보 비교

**발화 분석용** (정해진 형식의 결과를 안정적으로 출력하는 것이 중요)

| 모델 | 특징 | 평가 |
|---|---|---|
| **OpenAI gpt-4o-mini** ⭐선정 | 결과를 정해진 JSON 형식으로 강제하는 기능이 안정적, 응답 속도 빠름, 음성 인식(Whisper)과 동일 벤더 | 분석 역할에 필요한 조건을 모두 충족 |
| OpenAI gpt-4.1-mini | 지시 이행력 소폭 우위 | 차이가 결과 품질에 유의미하지 않음 |
| Claude Haiku 4.5 | 형식 강제 지원, 한국어 준수 | 품질 동급, 별도 벤더 계정·연동 추가 필요 |
| Google Gemini Flash | 형식 강제 지원 | 품질 동급, 별도 벤더 계정·연동 추가 필요 |

**캐릭터 질문 생성용** (캐릭터 말투 유지와 아동에게 적합한 표현이 중요)

| 모델 | 특징 | 평가 |
|---|---|---|
| **gpt-4o-mini** ⭐기본 | 서버가 유도할 요소를 지정해 주는 구조라 제약이 강해, 소형 모델로도 일관된 품질 확보 가능 | 기본값으로 채택 |
| gpt-4o | 캐릭터 말투의 자연스러움·연기력 우위 | 리허설에서 질문 품질이 부족하다고 판단되면 **설정 한 줄로 전환** |
| Claude Sonnet 5 | 캐릭터 유지·안전성 강점 | 품질 동급, 별도 벤더 추가 필요 |
| Google Gemini Pro | 동급 품질 | 별도 벤더 추가 필요 |

### 선정 이유

1. **역할에 맞는 모델 크기**: 판단 로직이 서버 프로그램으로 분리되어 있어 AI의 역할이 "관찰하고 보고하기 / 지정된 질문 만들기"로 명확합니다. 이 역할에는 응답이 빠른 소형 모델이 적합하며, 결과 형식을 강제하는 기능이 소형 모델의 약점(출력 형식 불안정)을 제거합니다.
2. **응답 속도가 사용 경험에 직결**: 아이가 말한 뒤 캐릭터가 대답하기까지 분석 → 판단 → 질문 생성 → 음성 합성이 순차로 이어지므로, 각 단계가 빠를수록 대화가 자연스럽습니다. gpt-4o-mini는 1초 미만 응답으로 이 조건을 충족합니다.
3. **단일 벤더 운영**: 음성 인식(Whisper)과 같은 OpenAI 계정·연동 방식을 재사용해 시스템 구성이 단순해지고 장애 지점이 줄어듭니다.
4. **품질 향상 경로 확보**: 역할별 모델을 설정값으로 분리해 두어, 캐릭터 질문의 품질을 높이고 싶으면 생성 모델만 상위 모델(gpt-4o)로 바꾸면 됩니다. 코드 수정 없이 설정 변경만으로 전환됩니다.

### 아동 안전 장치

어떤 모델을 쓰든 공통으로 적용되는 안전 장치를 둡니다.

- 질문 생성 시 연령에 적합한 어휘·금지 주제 규칙을 프롬프트에 명시
- 생성된 질문을 서버가 다시 검증 (길이 제한, 금칙어 필터)
- 대화 마무리 멘트는 사전에 확정한 고정 대사만 사용

---

## 3. 캐릭터 음성 (TTS) — 캐릭터의 목소리를 만든다

### 이 영역의 과제

방귀쟁이 며느리, 시아버지, 마을 이장 등 **여러 캐릭터가 서로 다른 목소리**로 말해야 하며, 어린이 청자에게 자연스러운 한국어 발음·억양이 필요합니다. 대사는 두 종류입니다.

- **고정 대사**: 장면 시작·마무리 멘트처럼 미리 정해진 대사 → 사전에 음성 파일로 만들어 두고 재생
- **가변 질문**: AI가 실시간으로 생성하는 질문 → 그 자리에서 음성으로 합성

### 후보 비교

| 후보 | 특징 | 캐릭터 목소리 구분 | 연동 난이도 | 평가 |
|---|---|---|---|---|
| **타입캐스트 API** ⭐선정 | 한국어 캐릭터·연기 보이스 수백 종 (아동·노인·사극 톤 포함), 감정 표현 조절 가능 | 캐릭터마다 다른 보이스를 배정하는 것만으로 완결 | 낮음 — 표준 API + 한국어 문서 | 전래동화 캐릭터에 맞는 연기 톤이 가장 풍부 |
| 수퍼톤 API | 감정 표현 품질 우수, 목소리 복제 기능 보유 | 보이스 라이브러리에서 배정 | 낮음 | 품질은 좋으나 보이스 다양성에서 타입캐스트 대비 이점 없음. 향후 성우 녹음 확보 시 유력한 전환 후보 |
| 네이버 클로바 보이스 | 발음·억양 안정성 최상급, 아동 보이스 보유 | 페르소나 보이스 배정 | 중간 — 클라우드 계정·인증 절차 필요 | 품질은 우수하나 도입 절차가 상대적으로 김 |
| 글로벌 서비스 (ElevenLabs, Google, Azure 등) | 다국어 범용 모델 | 가능 | 낮음~중간 | 한국어 억양의 부자연스러움, 캐릭터 연기 톤 부족으로 제외 |

### 선정 이유

1. **캐릭터 연기 톤 보이스가 가장 풍부**: 전래동화 캐릭터(젊은 여성/코믹, 노년 남성, 중년 남성)에 어울리는 목소리를 별도 제작 없이 프리셋 배정만으로 확보할 수 있습니다.
2. **한국어 품질과 사용 환경**: 한국 기업 서비스로 한국어 발음·억양 품질이 검증되어 있고, 한국어 개발 문서와 국내 서버 환경(응답 지연에 유리)을 갖추고 있습니다.
3. **어린이 청자 적합성**: 아동용 보이스와 감정 표현 파라미터를 지원해 이야기 몰입감을 높일 수 있습니다.

### 작동 방식

- **고정 대사**는 사전에 전량 음성 파일로 생성해 저장소에 올려두고 즉시 재생합니다. 실시간 호출이 없어 지연이 없습니다.
- **가변 질문**만 실시간으로 합성하되, 같은 문장이 반복 생성되는 경우를 대비해 한 번 만든 음성은 저장해 두고 재사용(캐싱)합니다.
- TTS 연동부도 교체 가능한 구조로 만들어, 향후 성우 녹음을 확보해 목소리 복제(수퍼톤)로 전환하더라도 **앱·서버 코드 변경 없이** 설정 교체로 완료됩니다.

---

## 전체 흐름 — 한 번의 대화 턴이 처리되는 과정

```
아이가 말하고 '보내기'를 터치
   1. [STT] 음성을 글자로 변환 (힌트 주입 → 실패 판정 → 교정)
   2. [AI 분석] 답에서 사고 요소·답변 유형을 찾아 보고
   3. [서버 프로그램] 부족한 요소 계산, 대화 계속/마무리 결정
   4-a. 계속  → [AI 생성] 부족 요소를 유도하는 캐릭터 질문 생성
              → 서버 검증 → [TTS] 캐릭터 목소리로 합성 → 재생
   4-b. 마무리 → 사전 제작된 고정 마무리 대사 재생
```

각 단계가 분리되어 있어 문제가 생기면 **어느 단계의 문제인지 바로 확인**할 수 있고, 특정 단계의 서비스를 교체해도 나머지는 영향을 받지 않습니다.

---

## 남은 확인 사항

- STT 실패 판정의 기준값(신뢰도 임계치)은 실제 시연 태블릿과 아이 목소리 샘플로 실측한 뒤 확정합니다.
- 리허설에서 캐릭터 질문 품질을 평가해, 필요 시 질문 생성 모델을 상위 모델로 전환합니다.
- 각 서비스의 요금·제공 조건은 변동될 수 있어 제출 전 공식 페이지에서 재확인합니다.
